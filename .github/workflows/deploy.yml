name: Deploy Site

on:
  push:
    branches:
      - master

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2.0.0
    - name: Install Dependencies
      run: npm i
    - name: Deploy site
      run: npm run site:deploy
