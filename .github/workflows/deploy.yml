name: Deploy Site

on:
  push:
    branches:
      - master

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
    - name: Install Dependencies
      run: npm i
    - name: Deploy site
      run: npm run site:deploy
